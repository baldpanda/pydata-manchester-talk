[tool.poetry]
name = "pydata_manchester_talk"
version = "0.1.0"
description = ""
authors = ["Your Name <you@example.com>"]
readme = "README.md"
packages = [
    { include = "pydata_manchester_talk" }
]

[tool.poetry.dependencies]
python = "^3.10"
haystack-ai = "^2.2.4"
boilerpy3 = "^1.0.7"
torch = "2.2.0"
sentence-transformers = "^3.0.1"
python-dotenv = "^1.0.1"


[tool.poetry.group.dev.dependencies]
ipykernel = "^6.29.5"
pytest = "^8.2.2"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
